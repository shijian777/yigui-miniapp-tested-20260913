# VS Code 与微信模拟器全流程测试记录

测试日期：2026-09-13。测试对象：本目录的小程序 1.3.0，本地账本模式。

## 测试环境

- VS Code 已安装在 `/Applications/Visual Studio Code.app`，并打开本项目。
- 官方 Node.js 24.21.0 LTS、npm 11.19.0 已安装。下载的 macOS ARM64 压缩包通过官方 SHASUMS256 校验。
- Node 安装在 `/Users/dodo/.local/share/node-v24.21.0-darwin-arm64`，命令位于 `/Users/dodo/.local/bin`。在 VS Code 的新终端中实际确认了版本和命令路径。
- 微信开发者工具 2.02.2608070，调试基础库 3.17.3，模拟设备 iPhone 12/13 (Pro)。
- 原有测试数据保留。本轮只新增明确命名的测试商品、客户、供应商和测试订单，没有真实收付款或对外发送消息。

## 本轮实际点击验证

| 场景 | 操作和观察结果 |
| --- | --- |
| 商品建档 | 新建 `VSVS全程测试纯棉上衣`，条码 `VS09131820`，米白/M，进价 40、售价 100、初始库存 0；保存返回后重新打开，输入仍在。 |
| 零库存补货 | 从商品进入进货，选规格，数量加到 5；合计 200，备注 `VS全程入库五件`。保存后从成功弹窗进入进货单，数量、价格、备注正确。单号 `JH2026091318241876`。 |
| 销售选品 | 按条码搜索，点击结果，再选颜色尺码加入购物车；修复后返回时清空搜索并收起结果列表，购物车保留选中商品。删除未提交的测试行也正常。 |
| 折扣与欠款 | 2 件 × 100，九折后应收 180，实收 50，欠款 130；输入销售备注后进入客户选择。 |
| 开单中新增客户 | 在客户选择页新建 `VS全程档案客户`，填写地址 `测试地址0913`、备注 `偏好米白M码，仅测试`、标签“回头客”；保存、选择后回到开单页，商品、折扣、实收和备注均保留。 |
| 提交与小票 | 保存销售单 `XS2026091318295023`，查看电子小票，店名、客户、米白/M、数量、九折优惠、180/50/130 金额及中文备注显示正确。 |
| 收回欠款 | 欠款台账显示 130；打开订单“补收余款”并选现金后，实收更新到 180，欠款归零，订单进入已结清列表。这里记录的是账务测试，没有实际资金转移。 |
| 退货取消 | 点击退货后先取消，订单仍为正常，退货按钮恢复可操作。 |
| 整单退货 | 再次确认退货后标记已退货，收款和再次退货按钮消失；该商品库存由 3 恢复为 5，首页销售和毛利归零，台账无待收欠款。原交易金额保留作历史记录。 |
| 客户资料与搜索 | 重新打开客户可读到原地址、备注、标签。添加测试电话 `13800000913` 和微信 `VSTEST_0913` 后保存返回；按电话和小写微信号均能搜到，输入无匹配字符串显示空状态，再改回微信号能恢复结果。 |
| 客户对账 | 客户“更多→查看订单流水”可进入对账单，中文姓名在修复后正常显示。退货记录仍可见，净销售、已收款、仍欠款汇总均为 0。 |
| 供应商 | 新建 `VS测试服装供应商`，填写联系人、测试电话和中文备注；保存自动返回，列表完整显示已保存信息。 |
| 统计切换 | 今日统计显示进货 250、进货单 3、退货单 3，净销售和毛利 0；切到昨天显示无记录，切回今天恢复对应数据。进货 250 包含此前测试的 50。 |
| 导入取消 | 点击导入恢复可打开文件选择窗口；取消后回到设置页，导出、导入及保存按钮恢复可用，账本记录数量保持原样。 |
| 滞销计算 | 修复后，90 天和 30 天条件均不再把今天刚入库的测试商品列为滞销。页面显示 0 条、库存 0、占款 0，并说明按首次入库/建档时间判断，返回首页正常。 |
| 备份生成与提示 | 新流程实际生成 `clothing-pos-backup-20260913-190627-mtzpl2cj-1.json` 并展示时间。再次点击保存/分享已不再报 TAP 手势错误，而是开发者工具明确返回“暂时不支持此 API 调试，请使用真机”。界面提示“备份已生成，请在手机微信中保存/分享”，文件和按钮保留；未声称分享成功。 |
| 重新编译 | 本轮多次重新载入后，已保存的商品、客户、订单与退货结果保留。曾出现一次工具级 `simulator launch failed`；后续重新编译恢复，复测时调试器错误数为 0，仍有基础库灰度、弃用 API 和资源预载提示。 |

## 本轮发现与修复

1. 销售搜索结果选规格后返回，搜索列表仍覆盖购物车：成功加购后清理搜索，并阻止旧查询回填；取消或库存不足时保留搜索。
2. 客户列表提示支持电话/微信，实际只搜索姓名：改为店主范围内的姓名、电话、微信联合搜索，保留分页、排序和特殊字符转义。
3. 从客户列表进入对账单，中文姓名显示为 `%E5...`：路由参数只解码一层，非法百分号编码原样保留，客户 ID 同样正确解码用于查询。
4. 备份生成后直接分享被微信 TAP 手势限制拦截：拆成生成和新点击分享两步，失败或取消保留文件；同秒重复生成不覆盖同名文件，账本内容变更后要求重新生成。已实际确认手势错误消除；最终分享受本机开发者工具能力限制，须真机验证。
5. 当天新建、刚入库且无有效销售记录的商品被列入“90 天滞销”：改按对应规格/整款首次有效入库时间，无入库证据时回退有效建档时间，达到阈值才纳入；未知、非法或未来日期不会被当成陈年库存。有销售的商品仍按最后未退货销售日期判断。页面将计数标为“规格数”，说明退货不计有效销售。

同时把对账页说明改成“汇总仅计未退货订单”，与实际计算口径一致，避免误读为重复扣减退货金额。

## 自动化覆盖与运行方式

按 `Command+Shift+B` 运行“小程序：完整自动化检查”，或在项目终端运行：

```sh
npm run test:full
```

任务已在 VS Code 中通过快捷键实际执行并正常退出，结果为 **202 项通过、0 失败、0 跳过**。检查了 **94 个 JS/JSON 文件、20 个页面**及事件/跳转；官方 WCC 编译 20 个模板、WCSC 编译 21 个样式均通过。日志保存到 `artifacts/vscode-test-results.txt`。

本轮新增 54 项回归：销售搜索返回 5 项、客户搜索 9 项、对账路由 9 项、备份分享 13 项、滞销年龄 18 项。各问题先复现失败，再修复至通过；另经独立只读审查未发现阻断项。

测试还覆盖了界面难以稳定制造的情况：双击重复提交、失败重试、并发销售最后一件、非法/空数量与金额、库存不足整单回滚、存储写入失败、跨页筛选、旧异步结果回填、备份覆盖/合并冲突/恢复失败及恢复后旧表单失效。测试使用隔离数据，不会改动模拟器账本。

## 明确的测试边界

- 这是当前已实现功能的本地营业流程验收；不代表所有手机型号、全部硬件和外部服务均已通过。
- 当前界面支持整单销售退货、一次补齐余款；部分退货、换货、进货退货、分次金额补收界面尚未提供。分次收款业务接口有自动化覆盖。
- 真实手机软键盘、摄像头扫码、扫码枪、蓝牙打印出纸、相册保存和发送给联系人仍需在相应设备上验证。本轮没有发送小票或账本给联系人。
- 导入覆盖、合并冲突和恢复回滚由隔离自动化测试验证；没有用覆盖恢复去替换当前模拟器账本。
- 本地账本不自动跨设备同步，支付方式仅记账。原包云函数尚未适配当前本地实现，本轮没有云部署或发布。

更早的修复和测试记录见 `20260913-替换与验收.md`；本记录补充了 VS Code 配置和后续实际操作。
