{
  "name": "yigui-miniapp",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-project.js",
    "test:full": "npm test && node scripts/check-project.js --require-compiler"
  },
  "engines": {
    "node": ">=18"
  }
}
